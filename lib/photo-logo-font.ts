import { Plus_Jakarta_Sans } from "next/font/google";

/** Distinctive wordmark weight for the header logo (text-only). */
export const photoLogoFont = Plus_Jakarta_Sans({
  subsets: ["latin"],
  weight: ["700"],
  display: "swap",
});
